
#include "Person.h"

#define _CRT_SECURE_NO_WARNINGS

Person::Person(const string name, const string tel, const string pc, const string email) {

	SetName(name); 
	SetTel(tel); 
	SetPC(pc); 
	SetEmail(email); 

	return; 
}
